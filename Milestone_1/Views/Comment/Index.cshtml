@model IEnumerable<Milestone_1.models.Comment>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tweets)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserData)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.commentText)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tweets.id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserData.UserDataId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.commentText)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
